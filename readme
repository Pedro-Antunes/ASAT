Bitset
	data <int>
	-> set(i)
	-> reset(i)
	-> flip(i)
	-> get(i)

Valoração = <Bitset>

Individuo
	id <int>
	val <Valoracao> 
	mem <list(Valoracao)>
	actv <Valoracao>
	PrMut <float>
	-> unique() # Retorna o número de Valorações únicas

Populacao
	elem <list(Individuo)>
	-> getIndividuo?access(i)

Evento
	kind <string>
	time <float>
	target <Pop>
	-> getKind()
	-> getTime)
	-> getTarget()

CAP
	sch <Heap>
	-> next()
	-> add
	-> replace(e)
	...

Formula
	varCount?N_var <int>
	size <int>
	clauses?form <list(list(int))>
	-> evaluate(Valoracao) # Retorna o Coeficiente de Adaptação
	-> getVarCount()

Simulador
	populacao; agenda;
	eventoAtual, 
	...

Comentários

Critica à qualidade do código: i.e. lista de bits ativos, minimo local